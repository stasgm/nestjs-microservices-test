{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "🚀 gateway",
      "command": "pnpm",
      "args": ["start:gateway:dev"],
      "options": {
        "cwd": "${workspaceFolder}",
      }
    },
    {
      "label": "🧶 lint",
      "command": "pnpm",
      "args": ["lint"],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    },
    {
      "label": "✨ Format",
      "command": "pnpm",
      "args": ["format"],
      "options": {
        "cwd": "${workspaceFolder}",
      }
    },
    {
      "label": "✅ Typecheck",
      "command": "pnpm",
      "args": ["typecheck"],
      "options": {
        "cwd": "${workspaceFolder}",
      }
    }
  ]
}
